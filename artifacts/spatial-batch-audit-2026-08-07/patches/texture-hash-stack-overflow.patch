diff --git a/src/cooker/TextureCookPreparation.cpp b/src/cooker/TextureCookPreparation.cpp
--- a/src/cooker/TextureCookPreparation.cpp
+++ b/src/cooker/TextureCookPreparation.cpp
@@
-            std::array<UCHAR, 1024 * 1024> buffer{};
+            std::vector<UCHAR> buffer(1024 * 1024);

Reason: remote commit a482212 placed a 1 MiB hash buffer on the default Windows
thread stack, causing 0xc00000fd before the first texture was written. Moving
the same byte buffer to heap storage changes allocation only, not cooked data.
